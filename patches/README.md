# Patch Ledger

Keep this directory for intentional changes made to OBS-derived source files.

Each patch should include:

- the OBS file touched
- why the patch exists
- whether it can be removed during the next OBS upgrade
- any platform-specific risk

The preferred state is for this directory to stay nearly empty.
